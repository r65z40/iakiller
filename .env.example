# Clé secrète pour les tokens JWT d'authentification admin
# IMPORTANT : changez cette valeur en production !
# Générer une clé : node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=iakiller-secret-change-me-in-production

# Identifiants administrateur par défaut
ADMIN_EMAIL=admin@iakiller.com
ADMIN_PASSWORD=admin123
